# Implementation Plans

Generated by the improve skill on 2026-07-09 (audit at commit `d0d0d2a`). Execute in the order below unless dependencies say otherwise. Each executor: read the plan fully before starting, honor its STOP conditions, and update your row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001  | Guard /api/site-info against SSRF | P1 | S | — | DONE — reviewed & approved. Commit `6d2ca6f` on branch `advisor/001-ssrf-guard-site-info` (in worktree `.claude/worktrees/agent-a830f6f63fc22bd17`), awaiting user merge. 5/5 new tests pass; build passes. Full-suite criterion waived: 39 auth/stripe test failures verified byte-identical WITHOUT the change (pre-existing, see below). |
| 002  | Patch high-severity dependency vulnerabilities | P1 | S | — | DONE — reviewed & approved. Commit `70ea0f2` on branch `advisor/002-dependency-security-patches` (worktree `.claude/worktrees/agent-ab72ee9b31cb64449`), awaiting user merge. Lockfile-only change; audit 15→5 vulns; defu/tmp/path-to-regexp/effect/brace-expansion gone; nodemailer patched within 8.x (3 of 4 advisories fixed). Accepted residuals: Next 14 cluster (needs next@16), uuid-via-exceljs, nodemailer GHSA-p6gq-j5cr-w38f (needs v9 — schedule minor follow-up). Tests identical to baseline; build passes. |
| 003  | Postgres-backed rate limiting for serverless | P1 | M | — | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- No hard dependencies, but land 001 and 002 before 003: all three touch `src/server.js`/`package-lock.json`, and the smaller diffs should merge first.
- 002 leaves two **accepted residuals**: the Next.js 14 advisory cluster (fix requires next@16, a breaking major — needs its own migration plan) and uuid-via-exceljs (fix would downgrade exceljs, breaking; vulnerable path likely unreachable from report generation).
- 003 must check the installed `express-rate-limit` major version if 002 landed first (Store interface changed in v7).

## Audit findings not planned (selected by user: security trio only)

Vetted and real, available for a future planning pass:

- **Scheduled crawls are a paid stub** — `CrawlSchedule` CRUD exists, no worker executes schedules (`vercel.json` has no `crons`). Product-critical.
- **Zero tests on plan limits, Stripe webhook idempotency, IDOR paths**; no CI workflow.
- **Stats computed 3× per crawl lifecycle** (`src/server.js:4650-4707` vs `4911-5070`); Excel export ignores stored `CrawlRun.stats`.
- **Project-limit check-then-act race** (`src/server.js:3267-3299`, no transaction).
- **Crawl runs in one serverless request, all pages in memory, no resume** — hard scale ceiling at 300s.
- **No lint/typecheck coverage of the 5.4k-line JS monolith**; `@types/react` 19 vs React 18 mismatch.
- **Dead weight**: `next-frontend/` orphan, `backup_neon.dump` committed at root, `scratch_styles.css`, ~18 audit MDs at root.
- **Crawl comparison + alerts** promised by plan tooltips, not implemented (direction).
- **CLAUDE.md claims a CMS module that has zero code** (direction/docs).
- **NEW (found during 001 execution): the existing test suite is not green on a clean DB** — `tests/auth.test.js` creates login users without `emailVerified: true`, so login returns 403 EMAIL_NOT_VERIFIED (`src/server.js:2771`) and 24/39 auth tests fail; `tests/stripe.test.js` webhook tests get 501 (likely missing `STRIPE_WEBHOOK_SECRET` in the dev env pull — it's Production-only in Vercel). Verified pre-existing at commit `d0d0d2a` with no working-tree changes. Small S-effort fix; should precede any plan that relies on `npm test` as a gate (e.g. 003).

## Findings considered and rejected

- **"Team member plan limits not enforced"** — false: enforced at `src/server.js:3552-3561` (`getTeamMaxMembers` + 403). Do not re-audit.
- **Stripe webhook signature verification, cookie flags, password hashing, ownership checks on deletes** — verified correctly implemented; no action.
- **"helmet/cors initialized but not applied"** — inconclusive subagent claim (MED confidence, cursory); if someone touches security middleware, verify `app.use` wiring then, not as standalone work.
